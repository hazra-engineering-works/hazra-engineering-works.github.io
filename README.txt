Hazra Engineering Works — Reference Match v23

This package intentionally uses the approved reference artwork as the desktop layout surface so the browser cannot reflow or stretch the hero, welder, engine van, service cards or gallery differently from the approved preview.

Upload BOTH items to the root of the GitHub Pages repository:
1) index.html
2) images/ folder

Important: Keep images/approved-homepage.png in the images folder.
